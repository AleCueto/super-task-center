import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'super-task-center',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
